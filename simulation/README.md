# Simulation

- `spice/` — LTspice / ngspice sources (.asc, .cir, .lib)
- `python/` — system-level, Monte Carlo, noise budgeting scripts
- `results/` — plots and reports with date + tool version + models used

Simulation does not prove low-current / leakage / layout-dependent performance.
