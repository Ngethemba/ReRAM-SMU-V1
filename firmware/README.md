# Firmware

STM32 (candidate: G431) firmware. Safe-state invariant: output disabled on boot/reset/fault.

- `src/` / `include/` — source
- `tests/` — host/unit tests
- `docs/` — firmware design notes
