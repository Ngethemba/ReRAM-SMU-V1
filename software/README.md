# Software

Host-side instrument control.

- `instrument-control/` — Python library + SCPI-like client
- `gui/` — optional GUI (future)
- `analysis/` — sweep analysis, plotting
- `tests/` — integration tests

Data export: CSV + raw with metadata; `measurements/raw/` is append-only.
