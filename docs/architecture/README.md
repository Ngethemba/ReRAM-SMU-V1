# Architecture

Authoritative block diagrams, power tree, signal chain, and grounding/isolation/guard strategy.

- `ARCHITECTURE.md` — system block diagram (added in Phase 2)
- Proposals here are **provisional** until a `DECISIONS.md` entry promotes them.
